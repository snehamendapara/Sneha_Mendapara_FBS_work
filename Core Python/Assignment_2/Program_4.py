#WAP to calculate area of triangle and rectangle

length = float(input("Enter Leanth of rectengle")) 
width = float(input("Enter width of rectengle"))

area_rect = length * width 

print(f"area of rectangle = {area_rect}")

#calculate area of triangle 
base = float(input("Enter base of triangle"))
height = float(input("Enter height of triangle"))

area_triangle = 0.5 * base * height 

print(f"area of triangle = {area_triangle}")




